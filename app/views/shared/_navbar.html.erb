<nav class="navbar navbar-default navbar-custom">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>

            <%= link_to new_petition_path, :class => "navbar-brand pls" do %>
            <i class="fa fa-pencil-square-o fa-2x" aria-hidden="true"></i>
            <div>Start Petition</div>
            <% end %>

        </div>

        <ul class="nav navbar-nav navbar-center midNav">
            <li>
                <%= link_to root_path do %>
                BRUIN<span>SPEAK<span>
                    <% end %>
                </li>
            </ul>

            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse navbar-right" id="bs-example-navbar-collapse-1">
                <!-- <ul class="nav navbar-nav navbar-left leftNav">
                <li><a href="#">Start Petition</a></li>
            </ul> -->
            <ul class="nav navbar-nav navbar-right rightNav">
                <li><%= form_tag search_path, :method => 'get' do %>
                    <%= text_field_tag :search, params[:search], placeholder: "What are you looking for?"%>
                    <%= button_tag do %>
                        <i class="fa fa-search" aria-hidden="true"></i>
                    <% end %>
                <% end %></li>
                <li><%= link_to "Explore", explore_path %></li>
                <% if current_user %>
                <li style="padding-top: 11px"><%= current_user.first_name + " " + current_user.last_name %></li>
                <% if current_user.profile_pic.url %>
                    <li><img src=<%= image_path(current_user.profile_pic.url) %> class="img-circle" width="50" alt="edit" /></li>
                <% else %>
                    <li><img src=<%= image_path("default_profile_picture.png") %> class="img-circle" width="50" alt="edit" /></li>
                <% end %>
                <li><%= link_to "Logout", destroy_user_session_path, :method => "delete" %></li>
                <% else %>
                <li><%= link_to "Login", new_user_session_path %></li>
                <% end %>
            </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>
